export const exercisePhoto = `Look at this exercise or workout screenshot/photo and extract relevant training notes.
Return ONLY valid JSON, no markdown:
{
  "notes": "string — weight used (e.g. '60 kg'), reps or sets visible, form cues, RPE, or any other useful info. '' if nothing relevant."
}`;
